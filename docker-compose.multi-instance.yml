# This file is generated by scripts/generate_compose_from_instances.py
services:
  bio-monitor:
    image: ${NEWS_MONITOR_IMAGE:-news-monitor:latest}
    build: .
    container_name: bio-monitor
    env_file:
      - .env
    environment:
      CONFIG_PATH: /app/instance/config.json
    volumes:
      - ./output:/app/output
      - ./instances/bio-monitor:/app/instance:ro
    restart: unless-stopped

  chem-monitor:
    image: ${NEWS_MONITOR_IMAGE:-news-monitor:latest}
    build: .
    container_name: chem-monitor
    env_file:
      - .env
    environment:
      CONFIG_PATH: /app/instance/config.json
    volumes:
      - ./output:/app/output
      - ./instances/chem-monitor:/app/instance:ro
    restart: unless-stopped
